<ul>
<%= @patient.appointments.each do |appointment| %>
  <li><%= prettify(appointment.appointment_datetime) %> with <%= link_to appointment.doctor.name, doctor_path(appointment.doctor) %></li>
<% end %>
</ul>
