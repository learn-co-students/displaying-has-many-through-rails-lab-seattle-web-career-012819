<h1>Seattle Grace Hospital</h1>
<h2>Doctor Index</h2>

<ul>
  <% @doctors.each do |doc| %>
    <li>
      <%= link_to doc.name, doctor_path(doc) %>
      has <%= pluralize(doc.patients.count,'patient') %>
    </li>
  <% end %>
</ul>
